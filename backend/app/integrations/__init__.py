"""Governed API & Integration Platform — Volume 70.

Controlled connection layer for external APIs, webhooks, OAuth
providers and connectors. PostgreSQL-authoritative metadata, secrets
via the existing EncryptionService, events via EventBus, lease-based
workers. Never an unrestricted proxy, credential store, or bypass.
"""
